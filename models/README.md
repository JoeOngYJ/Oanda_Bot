# Models Directory

Canonical location for promoted model artifacts.

## Layout

- `active/` - currently promoted models for runtime/paper use
- `archive/` - older promoted models kept for rollback/reference

## Current Active Model

- `active/multiframe_regime_model_20260228_194306.json`

## Promotion Notes

- Train/evaluate in `data/research/` first.
- Copy a validated artifact into `models/active/` when promoted.
- Keep timestamped filenames to preserve provenance.
